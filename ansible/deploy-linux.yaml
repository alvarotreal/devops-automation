- name: Copy and Start Go on Linux Server
  hosts: webservers
  become: yes
  tasks:
    - name: Copy Go Binary
      copy:
        src: ../todoapp
        dest: /home/alv
        mode: "0755"

    - name: Start Go Application
      command: ./todoapp